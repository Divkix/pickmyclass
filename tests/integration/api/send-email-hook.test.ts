import { describe, it } from 'vite-plus/test';

// Deleted in #351 — Clerk owns auth emails; Supabase send-email-hook removed.
describe.skip('POST /api/auth/send-email-hook (deleted — Clerk owns emails)', () => {
  it('placeholder — route deleted', () => {});
});
