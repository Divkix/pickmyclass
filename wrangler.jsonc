{
  "main": "./worker.ts",
  "name": "pickmyclass",
  "compatibility_date": "2025-10-21",
  "compatibility_flags": [
    // Enable Node.js API
    // see https://developers.cloudflare.com/workers/configuration/compatibility-flags/#nodejs-compatibility-flag
    "nodejs_compat",
    // Allow to fetch URLs in your app
    // see https://developers.cloudflare.com/workers/configuration/compatibility-flags/#global-fetch-strictly-public
    "global_fetch_strictly_public"
  ],
  "assets": {
    "directory": "dist/client",
    "binding": "ASSETS"
  },
  "placement": {
    "mode": "smart"
  },
  "kv_namespaces": [
    {
      "binding": "DISPOSABLE_DOMAINS_KV",
      "id": "5b1c731cca674372be70d72be05acb7b"
    },
    {
      "binding": "VINEXT_CACHE",
      "id": "7723b73644a5489ca54636ef7ef133fa"
    }
  ],
  // Custom domain configuration - uncomment and update for your deployment
  // "routes": [
  //   {
  //     "pattern": "your-domain.com",
  //     "custom_domain": true
  //   }
  // ],
  // IMPORTANT: Cron triggers require explicit deployment via 'wrangler triggers deploy'
  // This is automatically included in 'bun run deploy' script
  // Schedule: Every 30 minutes at :00 (even class numbers) and :30 (odd class numbers)
  // Daily at 4 AM UTC: Update disposable email domain list
  "triggers": {
    "crons": ["0,30 * * * *", "0 4 * * *"]
  },
  "queues": {
    "producers": [
      {
        "binding": "CLASS_CHECK_QUEUE",
        "queue": "class-check-queue"
      }
    ],
    "consumers": [
      {
        "queue": "class-check-queue",
        "max_batch_size": 5,
        "max_batch_timeout": 10,
        "max_concurrency": 20,
        "max_retries": 3,
        "retry_delay": 60,
        "dead_letter_queue": "class-check-dlq"
      }
    ]
  },
  "durable_objects": {
    "bindings": [
      {
        "name": "CRON_LOCK_DO",
        "class_name": "CronLockDO",
        "script_name": "pickmyclass"
      }
    ]
  },
  // Durable Objects migrations
  "migrations": [
    {
      "tag": "v2",
      "new_classes": ["CronLockDO"]
    }
  ],
  "observability": {
    "enabled": true,
    "head_sampling_rate": 1,
    "logs": {
      "enabled": true,
      "head_sampling_rate": 1,
      "persist": true,
      "invocation_logs": true
    }
  },
  "vars": {
    // Application configuration
    "MAX_WATCHES_PER_USER": "10",
    // UPDATE THESE VALUES for your deployment:
    "NOTIFICATION_FROM_EMAIL": "notifications@pickmyclass.app",
    "NEXT_PUBLIC_SITE_URL": "https://pickmyclass.app"
  }
  // IMPORTANT: Set these secrets via 'wrangler secret put <NAME>' or Cloudflare Dashboard:
  // - SUPABASE_SERVICE_ROLE_KEY
  // - ASU_API_BASE_URL (https://eadvs-cscc-catalog-api.apps.asu.edu/catalog-microservices/api/v1/search/classes)
  // - ASU_API_TOKEN (Bearer <token>)
  // - RESEND_API_KEY
  // - RESEND_WEBHOOK_SECRET
  // - CRON_SECRET (generate with: openssl rand -hex 32)
}
